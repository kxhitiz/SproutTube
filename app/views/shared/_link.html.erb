<% if person_signed_in? %>
  <%= "Welcome #{current_person.name}!" %>
<% end %>

<%= form_tag videos_path, :method => "get" do %>
  <%= text_field_tag :search, params[:search], :size => 15, :value => "Search Text Here" %>
  <%= submit_tag "Search Video!", :name => nil %>
<% end %>

