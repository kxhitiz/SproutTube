<h1>Browse Though All Videos</h1>
<h2>
<% @videos.each do |video| %>
    <%= link_to "#{video.title}", video_path(video) %>
    <%= "Hits: #{video.hits}" %>
    <%= "Reviews Count: #{review_count(video)}" %> <br />
<% end %>
</h2>


<%= will_paginate @videos %>
<%# page_entries_info @videos %>

